{
  "name": "chrome-virtual-keyboard",
  "version": "3.0.6",
  "type": "module",
  "description": "",
  "main": "index.js",
  "scripts": {
    "build": "node build.js",
    "build:firefox": "node build.js --firefox",
    "build:test": "node build.js --test",
    "watch": "node build.js --watch",
    "watch:firefox": "node build.js --watch --firefox",
    "format": "pnpm exec biome check --write .",
    "format:check": "pnpm exec biome check .",
    "lint": "pnpm exec biome lint .",
    "lint:fix": "pnpm exec biome lint --write .",
    "package": "pnpm run build && cd dist && zip -r ../chrome-virtual-keyboard.zip . -x \".DS_Store\"",
    "package:firefox": "pnpm run build:firefox && cd dist && zip -r ../smartkey-firefox.xpi . -x \".DS_Store\"",
    "test": "pnpm run test:unit && pnpm run test:e2e",
    "test:unit": "pnpm exec vitest run",
    "test:e2e": "pnpm exec playwright test",
    "test:ui": "pnpm exec playwright test --ui",
    "test:headed": "pnpm exec playwright test --headed",
    "test:debug": "pnpm exec playwright test --debug",
    "test:install": "pnpm exec playwright install chromium --with-deps",
    "test:report": "pnpm exec playwright show-report",
    "coverage": "pnpm run coverage:all",
    "coverage:unit": "rm -rf coverage-unit && pnpm exec vitest run --coverage",
    "coverage:e2e": "rm -rf coverage-e2e .coverage-data && COVERAGE=1 pnpm exec playwright test && node --max-old-space-size=6144 scripts/merge-coverage.js",
    "coverage:all": "rm -rf coverage coverage-unit coverage-e2e .coverage-data && pnpm run coverage:unit && COVERAGE=1 COVERAGE_SKIP_REPORT=1 pnpm exec playwright test && node --max-old-space-size=6144 scripts/merge-coverage.js",
    "coverage:summary": "node scripts/coverage-summary.js"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "packageManager": "pnpm@10.27.0",
  "devDependencies": {
    "@biomejs/biome": "^2.3.10",
    "@playwright/test": "^1.57.0",
    "esbuild": "^0.27.2",
    "happy-dom": "^20.0.11",
    "http-server": "^14.1.1",
    "monocart-coverage-reports": "^2.12.9",
    "vitest": "^4.0.16",
    "vitest-monocart-coverage": "^4.0.1"
  },
  "dependencies": {
    "@huggingface/transformers": "^3.8.1",
    "@ricky0123/vad-web": "^0.0.30",
    "fflate": "^0.8.2",
    "vosk-browser": "^0.0.8"
  }
}
